// This is where we store the JWT secret key and other configurations. In real life, you want to store it in environment variable instead.
export const JWT_SECRET = "secret"
export const JWT_EXPIRATION = "10m"
